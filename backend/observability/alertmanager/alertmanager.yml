global:
  resolve_timeout: 5m

route:
  group_by: ["alertname"]
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 2m
  receiver: "discord"

receivers:
  - name: "discord"
    webhook_configs:
      - url: "http://discord-webhook-adapter:9094"
        send_resolved: true
